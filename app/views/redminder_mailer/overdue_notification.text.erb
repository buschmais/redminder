<%= l(:redminder_mail_body) %>:

<% @issues_by_project.each do |project, issues| -%>
<%= project %>:
<% issues.each do |issue| -%>
* <%= "#{issue.tracker} ##{issue.id}: #{issue.subject}" %>
  <%= url_for(:controller => 'issues', :action => 'show', :id => issue, :only_path => false) %>
<% end -%>

<% end -%>
